# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                                       App.Index

GET     /radio                                                  Radio.Index
GET     /radio/maintain                                         Radio.Maintain
POST    /radio/query                                            Radio.Query
POST    /radio/ota/create                                       Radio.OtaCreate
GET     /radio/release                                          Radio.ReleaseIndex
GET     /radio/release/:fp                                      Radio.Release

GET     /stat                                                   Stat.Index
GET     /stat/self                                              Stat.Self
GET     /stat/all                                               Stat.All

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

#--the path should be changed in other environment
GET     /static/*filepath                       Static.Serve("/home/jianmin/OTA/release/radio_ota")

# Catch all
#*       /:controller/:action                    :controller.:action
